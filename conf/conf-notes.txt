Supported target is:
    core-image-minimal

Other targets expected to run:
    core-image-sato
    meta-toolchain
    meta-ide-support

You can also run generated qemu images with a command like
'../runqemu.openfiles`

